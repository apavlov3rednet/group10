export default function Table({children, ...props}) {
    const header = props.data[0];

    return (
        <table>
            <thead>
                <tr>
             </tr>
            </thead>
        </table>
    )
}